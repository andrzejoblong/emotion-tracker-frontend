import React from 'react';

const Header = () => {
  return (
    <header>
      <nav>
        <h1>Emotion Tracker</h1>
        {/* Add navigation links if needed */}
      </nav>
    </header>
  );
};

export default Header;
